3710. Maximum Partition Factor

Split the n points into exactly two non-empty groups. The partition factor of a split is the minimum Manhattan distance among all unordered pairs of points that lie in the same group. ()==> bipertite

maximum of minimum ()==> binary search on answer

